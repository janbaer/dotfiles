# Git commit rules

- Commit messages should alwas start with a ticket number, but only if the current directory is in a sub-directory of a directory with the name `check24`.
- The ticket number always starts with `VERBU-` followed bu 5 numbers.
- If git current branch is a feature branch, you can guess the ticket from the branch-name.
- Never add comment like `Generated by Claude` or `Generated by Claude AI` in the commit message.
